version: "3.9"

services:
  app:
    container_name: ringo-backend
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: npm run dev